{
  "name": "hrms-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "vercel-build": "npm run build",
    "preview": "vite preview",
    "start": "serve -s dist"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "serve": "^14.2.0"
  }
}